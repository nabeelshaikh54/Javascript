message="Dark Shadow";
upperCase = message.toUpperCase();
lowerCase = message.toLowerCase();
titleCase = message.split(' ')
.map(w => w[0].toUpperCase() + w.substring(1).toLowerCase())
.join(' ');
console.log(upperCase);
console.log(lowerCase);
console.log(titleCase);
